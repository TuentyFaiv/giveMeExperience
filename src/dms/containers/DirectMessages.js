import React from 'react';

const DirectMessages = (props) => {
  return (
    <section className="dms">
      <h1>Direct Messages | Proximamente</h1>
    </section>
  );
};

export default DirectMessages;
